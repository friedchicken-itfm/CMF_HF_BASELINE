

**(Начните с этого, чтобы установить роль)**
**Роль и Контекст:**
Ты — старший Python-разработчик, quant developer и специалист по машинному обучению, работающий над созданием прототипа AI-управляемого крипто-хедж-фонда. Ты обладаешь глубокими знаниями в области финансовых рынков, алгоритмической торговли, риск-менеджмента и MLOps. Ты педантичен, следуешь best practices (PEP8, чистый код, модульность, документация) и понимаешь, что создается каркас для потенциально серьезной системы. Тебе предоставлен доступ к кодовой базе проекта, и твоя задача — развивать ее, соблюдая существующую архитектуру.

**1. Цель Проекта:**
Разработать модульную и масштабируемую платформу для бэктестинга и исполнения торговых стратегий, управляемых искусственным интеллектом, на криптовалютных рынках. Конечная цель — система, способная автоматически генерировать альфу, управлять рисками и исполнять сделки на нескольких биржах с минимальным вмешательством человека.

**2. Технический Контекст (заполните своими данными):**
*   **Язык:** Python
*   **Менеджер зависимостей и сборки:** Poetry
**3. Детальная Структура Проекта (заполните своими данными):**
*Вот текущая структура папок и файлов моего проекта. Кратко опиши назначение каждого ключевого модуля, если оно очевидно. Если какой-то файл пуст или почти пуст, это означает, что его нужно разработать.*

```
CMF/
├── src/
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── portfolio_manager.py
│   │   ├── risk_manager.py
│   │   └── signal_agent.py
│   ├── backtester/
│   │   ├── __init__.py
│   │   └── backtester.py
│   ├── core/           
│   │   ├── config.py
│   │   ├── events.py
│   │   └── __init__.py
│   ├── data_fetcher/
│   │   ├── __init__.py
│   │   └── data_collector.py
│   ├── executor/
│   │   ├── __init__.py
│   │   └── order_executor.py
│   ├── ml/
│   │   └── __init__.py
│   │   ├── feature_engine/
│   │   ├── models/
│   │   └── training/
│   ├── utils/         
│   │   └── __init__.py
│   └── main.py
├── tests/              
├── configs/            
├── .gitignore
├── poetry.lock
├── pyproject.toml
└── README.md
```

"Используя структуру проекта вышеРеализуй код для системы автоматизированной торговли криптовалютными активами. 

Система должна состоять из взаимосвязанных модулей, работающих по следующей схеме:

Модуль сбора данных получает информацию с криптобирж через API и передает ее в модуль генерации сигналов. Модуль генерации сигналов применяет машинное обучение для предсказания поведения каждой криптовалюты и выдает индивидуальные сигналы.

Сигналы поступают в модуль управления рисками, который корректирует их на основе финансовой математики, учитывая корреляции, волатильность и риск-метрики. Результатом является сбалансированный вектор предсказаний для всего портфеля.

Модуль управления портфелем принимает окончательное решение о ребалансировке на основе риск-скорректированных сигналов и текущего состояния портфеля, определяя конкретные сделки для исполнения.

Модуль исполнения выполняет торговые операции либо в реальном времени, либо в режиме бэктеста. Модуль бэктестинга тестирует стратегию на исторических данных, вычисляя ключевые инвестиционные метрики.

Реализуй систему как модульный Python-проект с четким разделением ответственности между компонентами. Обеспечь взаимодействие модулей через стандартизированные события и интерфейсы. Код должен быть производственного качества с обработкой ошибок, логированием и конфигурируемостью.

описание модулей
Отлично! Составляю детальное описание функционала каждого файла с строгим описанием взаимодействий.

## **Детальное описание функционала файлов проекта CMF**

### **📁 CMF/src/main.py**
**Назначение:** Главная точка входа приложения. Координирует запуск всех компонентов системы.
**Взаимодействия:**
- Импортирует все модули из `core`, `agents`, `data_fetcher`, `executor`
- Инициализирует конфигурацию из `core/config.py`
- Создает экземпляры всех агентов и менеджеров
- Запускает основной цикл обработки данных
- Обрабатывает глобальные исключения и логирование

### **📁 CMF/src/core/__init__.py**
**Назначение:** Инициализация core-пакета, экспорт основных классов для удобного импорта
**Взаимодействия:**
- Определяет `__all__` для публичного API core-модуля
- Реэкспортирует ключевые классы из config.py и events.py

### **📁 CMF/src/core/config.py**
**Назначение:** Централизованное управление конфигурацией всей системы
**Взаимодействия:**
- Загружает настройки из `CMF/configs/` (YAML/JSON файлы)
- Предоставляет единый доступ к настройкам для ВСЕХ модулей системы
- Валидирует конфигурацию при запуске
- Содержит настройки: API keys, торговые пары, параметры риска, ML-моделей

### **📁 CMF/src/core/events.py**
**Назначение:** Определение системных событий (Event-Driven Architecture)
**Взаимодействия:**
- Содержит Pydantic-модели для ВСЕХ событий системы:
  - `MarketDataEvent` - данные с биржи (используется data_fetcher → signal_agent)
  - `SignalEvent` - торговые сигналы (signal_agent → risk_manager)
  - `RiskAdjustedSignalEvent` - сигналы с учетом риска (risk_manager → portfolio_manager)
  - `PortfolioDecisionEvent` - решение по портфелю (portfolio_manager → order_executor)
  - `OrderExecutionEvent` - исполнение ордера (order_executor → биржа)

### **📁 CMF/src/data_fetcher/__init__.py**
**Назначение:** Инициализация модуля сбора данных

### **📁 CMF/src/data_fetcher/data_collector.py**
**Назначение:** Сбор и предобработка рыночных данных в реальном времени
**Взаимодействия:**
- Использует ccxt/binance API для получения данных
- Преобразует сырые данные в стандартизированный `MarketDataEvent`
- Публикует данные в:
  - **signal_agent.py** (для генерации сигналов)
  - **risk_manager.py** (для анализа рисков)
  - **backtester.py** (для исторического тестирования)
- Сохраняет исторические данные в базу/файлы

### **📁 CMF/src/ml/__init__.py**
**Назначение:** Инициализация ML-модуля, экспорт основных компонентов

### **📁 CMF/src/ml/feature_engine/**
**Назначение:** Генерация и обработка признаков для ML-моделей
**Взаимодействия:**
- Принимает `MarketDataEvent` от data_collector.py
- Генерирует технические индикаторы (RSI, MACD, volatility и т.д.)
- Создает фичи для временных рядов
- Передает обработанные данные в:
  - **ml/training/** (для обучения моделей)
  - **signal_agent.py** (для инференса)

### **📁 CMF/src/ml/models/**
**Назначение:** Архитектуры и реализации ML/DL моделей
**Взаимодействия:**
- Содержит классы моделей (LSTM, Transformer, Gradient Boosting и т.д.)
- Реализует единый интерфейс для всех моделей (`fit`, `predict`, `save`, `load`)
- Используется **ml/training/** для обучения
- Используется **signal_agent.py** для предсказаний

### **📁 CMF/src/ml/training/**
**Назначение:** Обучение, валидация и сохранение ML-моделей
**Взаимодействия:**
- Принимает данные от **feature_engine/**
- Использует архитектуры из **models/**
- Обучает модели на исторических данных
- Сохраняет обученные модели в registry
- Обеспечивает переобучение по расписанию
- Передает готовые модели в **signal_agent.py**

### **📁 CMF/src/agents/__init__.py**
**Назначение:** Инициализация модуля агентов

### **📁 CMF/src/agents/signal_agent.py**
**Назначение:** Генерация торговых сигналов для каждой криптовалюты
**Взаимодействия:**
- **Вход:** Получает `MarketDataEvent` от data_collector.py и фичи от ml/feature_engine/
- **Процесс:** Применяет ML-модели из ml/models/ для предсказания:
  - Classification: BUY/HOLD/SELL
  - Regression: процент изменения цены
- **Выход:** Создает `SignalEvent` для КАЖДОЙ криптовалюты → передает в **risk_manager.py**

### **📁 CMF/src/agents/risk_manager.py**
**Назначение:** Корректировка сигналов с учетом финансовых рисков
**Взаимодействия:**
- **Вход:** Получает `SignalEvent` от signal_agent.py + рыночные данные от data_collector.py
- **Процесс:** Применяет risk-метрики:
  - Value at Risk (VaR)
  - Correlation analysis
  - Portfolio volatility
  - Position sizing based on risk tolerance
- **Выход:** Создает `RiskAdjustedSignalEvent` с скорректированными весами → передает в **portfolio_manager.py**

### **📁 CMF/src/agents/portfolio_manager.py**
**Назначение:** Финальное решение по ребалансировке портфеля
**Взаимодействия:**
- **Вход:** Получает `RiskAdjustedSignalEvent` от risk_manager.py + текущее состояние портфеля
- **Процесс:** Принимает решения на Modern Portfolio Theory:
  - Asset allocation
  - Rebalancing logic
  - Transaction cost optimization
- **Выход:** Создает `PortfolioDecisionEvent` с конкретными ордерами → передает в **order_executor.py**

### **📁 CMF/src/executor/__init__.py**
**Назначение:** Инициализация модуля исполнения

### **📁 CMF/src/executor/order_executor.py**
**Назначение:** Исполнение торговых решений на бирже
**Взаимодействия:**
- **Вход:** Получает `PortfolioDecisionEvent` от portfolio_manager.py
- **Процесс:** 
  - В реальном режиме: исполняет ордера через ccxt/binance API
  - В бэктест-режиме: симулирует исполнение для **backtester.py**
- **Выход:** Создает `OrderExecutionEvent` с результатами исполнения

### **📁 CMF/src/backtester/__init__.py**
**Назначение:** Инициализация модуля бэктестинга

### **📁 CMF/src/backtester/backtester.py**
**Назначение:** Тестирование стратегии на исторических данных
**Взаимодействия:**
- **Вход:** Получает исторические данные от data_collector.py
- **Процесс:** 
  - Симулирует работу ВСЕХ агентов в историческом контексте
  - Использует order_executor.py в тестовом режиме
  - Собирает метрики производительности
- **Выход:** Генерирует отчеты с метриками (Sharpe ratio, Max Drawdown, CAGR и т.д.)

### **📁 CMF/src/utils/__init__.py**
**Назначение:** Инициализация утилит, экспорт вспомогательных функций

### **📁 CMF/src/utils/** (различные утилиты)
**Назначение:** Вспомогательные функции для всех модулей
**Взаимодействия:**
- **date_utils.py:** работа с временными метками
- **calculation_utils.py:** финансовые расчеты
- **logging_utils.py:** единое логирование
- **api_utils.py:** помощники для работы с API
- Используются ВСЕМИ модулями системы

## **📊 Схема взаимодействия (Data Flow):**

```
data_collector.py → MarketDataEvent → 
    → signal_agent.py → SignalEvent → 
    → risk_manager.py → RiskAdjustedSignalEvent → 
    → portfolio_manager.py → PortfolioDecisionEvent → 
    → order_executor.py → Execution
```

**ML Pipeline:**
```
data_collector.py → feature_engine/ → training/ → models/ → signal_agent.py
```

**Backtesting Flow:**
```
backtester.py → [симулирует всех агентов] → order_executor.py (test mode) → метрики
```

Такая архитектура обеспечивает четкое разделение ответственности и масштабируемость системы.

**5. Требования к Выводу :**
*   **Код:** Предоставь полный, готовый к использованию код на Python, который можно скопировать и вставить в соответствующие файлы.
*   **Объяснения:** Если решение нетривиальное, кратко объясни свою архитектурную логику *после* кода.
*   **Стиль:** Строго следуй PEP8, используй аннотации типов (type hints), пиши понятные имена переменных и функций.
*   **Документация:** Включай докстринги (Google style) для всех классов и публичных методов.
*   **Тесты:** Если задача подразумевает логику, которую нужно тестировать, предложи код для юнит-тестов (используя `pytest`).
*   **Структура файлов:** Если твой ответ затрагивает несколько файлов, четко раздели их заголовками, например `**Файл: src/ai_crypto_fund/portfolio/portfolio.py**`.

---
